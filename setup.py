from distutils.core import setup

setup(
  name = 'gree-python-api',
  packages = ['gree-python-api'], # this must be the same as the name above
  version = '0.1',
  description = 'Python implementation of the Gree AC API',
  author = 'Asaf Biton',
  author_email = 'bit.asaf@gmail.com',
  url = 'https://github.com/peterldowns/mypackage', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['gree', 'ac', 'device', 'api'], # arbitrary keywords
  classifiers = [],
)